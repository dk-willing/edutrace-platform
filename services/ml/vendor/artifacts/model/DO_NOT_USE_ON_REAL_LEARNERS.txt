This model was trained on SIMULATED data generated by edutrace/simulate/.

It has never seen a real student record. Its accuracy on real learners is
unknown and untested.

It is included so the API, the explanation layer and the demo can run out of
the box. It is a fixture, not a predictor.

Retrain on real data before scoring any real child. See STATUS.md.
